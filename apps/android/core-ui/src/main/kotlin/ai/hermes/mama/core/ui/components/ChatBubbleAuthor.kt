package ai.hermes.mama.core.ui.components

/** Quién habla en la burbuja (mockup Chat). */
enum class ChatBubbleAuthor {
    /** Burbuja a la derecha, fondo primaryContainer, esquina propia 6 dp. */
    User,

    /** Burbuja a la izquierda, fondo surface con borde fino, esquina propia 6 dp. */
    Hermes,
}
